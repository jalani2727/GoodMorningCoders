-- create table dbName (
--    id serial primary key,
-- );



create table comments (
    id serial primary key,

);


create table userinfo (
    id serial primary key,

);


create table topics(    
    id serial primary key,
    
);